/* --- Inference Type --- */

// The compiler tries to infer the type using the 'var' keyword
// once assigned, you cannot reassign to another type

// It's considered a good practice to let the compiler infer the type

var exampleStr = "hello world";
var exampleNum = 10;

Console.WriteLine(exampleStr);
Console.WriteLine(exampleNum);