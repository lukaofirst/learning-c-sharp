namespace enums
{
    public enum WeekDayEnum
    {
        Sunday, // by default, it starts with 0 and incremented by 1
        Monday,
        Tuesday,
        Wednesday,
        Thursday,
        Friday,
        Saturday
    }
}
